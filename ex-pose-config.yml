projects:  
  projectPath:
    inline:
      FOO: BAR
    openshift:
      - clusterURL:   https:/foo.bar
        namespace:    Foobar
        mapping:
          secret:
            USERNAME: username  # ENV : part-of-secert
            PASSWORD: password 

